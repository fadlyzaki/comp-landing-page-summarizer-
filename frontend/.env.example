# Frontend environment variables

# ADK API Server URL — leave empty for local dev (Vite proxy handles it)
# For production, set to the deployed backend Cloud Run URL
VITE_ADK_API_BASE_URL=

# ADK app name (must match the agent directory name in the backend)
VITE_ADK_APP_NAME=competitor_summarizer
